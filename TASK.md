# 2026-05-10 夜间探索任务

## Task 1: 多表格关联发现原型（探索性）
- Given: 项目目录下存在 2-5 张 CSV/Excel 表格（参考 csv-data 文件夹）
- When: 运行独立分析脚本时
- Then: 
  1. 自动检测表格间的潜在关联（列名相似、数据内容重叠、统计分布相似、疑似外键）
  2. 输出一份可读的关联报告（JSON 或简易 Graph 描述，节点=表/列，边=关联类型+置信度）
  3. 脚本可通过命令行直接运行并看到结果
- 探索范围:
  - 允许尝试多种启发式策略（Jaccard 相似度、数据指纹、取值范围重叠率等）
  - 不要求 100% 准确，先求"能跑出有意义的关联建议"
- 交付物: `experiments/table_graph_prototype.py`（独立脚本，不耦合业务代码）
- 限制:
  - 严禁修改现有文件读取核心逻辑（如 `file_reader.py` 或等价模块）
  - 严禁改动数据库 schema、知识库表结构、现有模型定义
  - 如果无 `experiments/` 目录请新建，所有探索代码必须放这里
  - 只处理 CSV/Excel，不要扩展其他格式
  - 今晚不要求集成到主流程，原型可运行即可

## Task 2: 全局拖拽上传交互优化
- Given: 用户在对话/主界面按住文件拖拽到浏览器窗口
- When: 在指定区域（如对话输入区上方或侧边栏）松开鼠标时
- Then:
  1. 触发上传流程（复用现有上传 API，不要重写底层请求）
  2. 上传成功后自动将文件存入知识库（复用现有知识库入库逻辑）
  3. 页面显示上传进度条/动画和成功提示，失败时显示错误原因
  4. 上传完成后对话区域能识别到该文件（如显示为附件标签）
- 交付物: 前端组件修改，建议新建 `src/components/GlobalDropZone.tsx`（或 Vue 等价文件）
- 限制:
  - 必须保留现有"文件页上传"功能，不得破坏原有入口
  - 不要修改后端上传接口的 URL、参数、返回值（除非现有接口确实不支持自动入库而必须微调）
  - 拖拽热区优先覆盖对话/主界面，不要全局拦截（避免影响现有按钮点击）
  - 使用现有 UI 组件库/样式方案，不要引入新的 CSS 框架
  - 今晚只做前端交互，不处理复杂冲突策略（如同名文件覆盖）

## STOP
- 完成以上两个任务后停止，不要自我发挥
- 不要执行任何 git 命令（add/commit/push）
- 不要优化与这两个任务无关的代码
- 如果某个任务 1 小时内无法完成可用原型，停止该任务并在 `experiments/` 下创建 `TASK1_DESIGN.md` 记录思路与卡点



# 2026-05-16 夜间探索任务

## Task 1: 幕后图谱构建异步任务（架构融合）

* Given: 用户通过“档案柜”上传了新表格文件，或系统检测到工作区内存在文件变动
* When: 触发文件变更/入库事件时
* Then:
1. 异步触发后台任务，静默运行多表格关联分析。
2. **核心逻辑整合**：参考并整合 `experiments/` 目录下已有的原型脚本逻辑（如截图中的 `minhash_*.py`, `table_gr*.py`, `semantic*.py` 等），将其核心算法提炼为生产可用的模块。
3. 自动构建数据版图，并将生成的元数据图谱（包含表节点、列节点和高置信度的关系边）缓存为当前工作区的全局数据地图（如 `report.json`）。
4. 执行轻量化保护逻辑：识别文件大小，超过既定体积阈值的文件仅读取前 N 行进行采样，避免引发后台卡顿或 OOM。


* 探索范围:
* 文件大小阈值与采样行数策略可以作为配置常量进行测试。
* 重点保证任务的静默性与低开销，不影响原有文件上传的主流程响应。


* 交付物: `backend/tools/schema_profiler.py`（封装图谱构建与轻量化缓存逻辑，并融合已有的实验脚本）及相关的异步触发点代码。
* 限制:
* 纯后端逻辑开发，严禁涉及任何前端改动。
* 必须加入文件体积阈值和采样逻辑，严禁无限制加载大文件。
* 生成的图谱数据作为内部缓存处理，不需要为图谱暴露直接对外下载的独立 API。



## Task 2: ReAct Agent 数据图谱工具扩展（赋予上帝视角）

* Given: 用户发起复杂的、可能涉及多个表格关联的数据分析提问
* When: Agent 进入 Thought 阶段，需要理解全局数据库表结构与潜在关联时
* Then:
1. Agent 能够自动调用新注册的核心 Tool：`get_database_schema_and_relations`。
2. 该 Tool 读取 Task 1 中缓存的全局数据地图，并作为 Context Provider 向 Agent 仅返回提炼后的 `summary` 和 `edges`（例如：“已知表 A 和表 B 存在疑似外键关系，包含率达 95%”）。
3. 配合 Auto Analysis，使 Agent 能够根据注入的图谱上下文，在沙箱中一次性生成高准确率的联表代码（如 `pd.merge` 且包含正确的关联键）。


* 探索范围:
* 调优 Tool 的 Prompt 描述，确保大模型明确知道何时该调用此工具。
* 优化向 Agent 注入 Context 的文本排版，使大模型最易于解析。


* 交付物: Agent 新 Tool 的定义代码、注册逻辑，以及相关的 Prompt 更新。
* 限制:
* 纯后端逻辑开发，不涉及前端交互。
* 该 Tool 严禁返回原始表格内容数据，只能返回元数据（结构和关系）。
* 必须复用现有的 ReAct Agent 循环和安全沙箱机制，不要重写底层推导逻辑。



## STOP

* 完成以上两个任务后停止，不要自我发挥
* 不要执行任何 git 命令（add/commit/push）
* 不要优化与这两个任务无关的代码
* 如果某个任务 1 小时内无法完成可用原型，停止该任务并在 `backend/tools/` 目录下创建 `TASK_DESIGN.md` 记录思路与卡点